{
  "name": "agent-lattice",
  "displayName": "Agent Lattice - A Visual Workflow Editor for AI Code Automation",
  "description": "Build complex AI code automation workflows visually with Agent Lattice. Connect AI agents, APIs, and tools in a drag-and-drop interface to create powerful coding assistants.",
  "version": "1.0.0",
  "publisher": "haym23",
  "icon": "resources/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/haym23/agent-lattice.git"
  },
  "license": "AGPL-3.0-or-later",
  "keywords": [
    "ai",
    "agent",
    "workflow",
    "claude",
    "claude-code",
    "cli",
    "codex",
    "copilot",
    "opencode"
  ],
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "AI",
    "Visualization"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cc-wf-studio.openEditor",
        "title": "CC Workflow Studio: Open Editor",
        "icon": {
          "light": "resources/icon.png",
          "dark": "resources/icon.png"
        }
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "cc-wf-studio.openEditor",
          "group": "navigation"
        }
      ]
    },
    "customEditors": [
      {
        "viewType": "cc-wf-studio.workflowPreview",
        "displayName": "Workflow Preview",
        "selector": [
          {
            "filenamePattern": "**/.vscode/workflows/*.json"
          }
        ],
        "priority": "default"
      }
    ],
    "configuration": {
      "title": "CC Workflow Studio",
      "properties": {
        "cc-wf-studio.ai.schemaFormat": {
          "type": "string",
          "enum": [
            "json",
            "toon"
          ],
          "default": "toon",
          "description": "Schema format for AI prompts. 'toon' reduces token consumption by ~23%."
        },
        "cc-wf-studio.ai.collectMetrics": {
          "type": "boolean",
          "default": false,
          "description": "Collect and log metrics for A/B comparison of schema formats. Note: Prompt format is always 'toon'."
        }
      }
    }
  },
  "activationEvents": [
    "onUri",
    "onCustomEditor:cc-wf-studio.workflowPreview"
  ],
  "scripts": {
    "vscode:prepublish": "npm run build",
    "generate:toon": "npx tsx scripts/generate-toon-schema.ts",
    "generate:editing-flow": "npx tsx scripts/generate-editing-flow.ts",
    "build": "npm run generate:toon && npm run generate:editing-flow && npm run build:webview && npm run build:extension",
    "build:standalone": "cd apps/standalone && npm run build",
    "build:dev": "npm run build:webview:dev && npm run build:extension:dev",
    "build:webview": "cd src/webview && npm run build",
    "build:webview:dev": "cd src/webview && npm run build:dev",
    "build:extension": "vite build --config vite.extension.config.ts",
    "build:extension:dev": "vite build --config vite.extension.config.ts --mode development",
    "compile": "tsc -p ./",
    "watch": "vite build --config vite.extension.config.ts --watch",
    "watch:webview": "cd src/webview && npm run dev",
    "lint": "biome lint .",
    "lint:standalone": "cd apps/standalone && npm run lint",
    "format": "biome format --write .",
    "format:standalone": "cd apps/standalone && npm run format",
    "check": "biome check --write .",
    "check:standalone": "cd apps/standalone && npm run check",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "cd src/webview && npm run test",
    "test:integration": "vscode-test",
    "test:e2e": "wdio run wdio.conf.ts",
    "test:standalone": "cd apps/standalone && npm run test",
    "verify:no-vscode-in-standalone": "rg -n \"from 'vscode'|acquireVsCodeApi|vscode\\.postMessage\" apps/standalone/src && exit 1 || exit 0",
    "ci:standalone": "npm run format:standalone && npm run lint:standalone && npm run check:standalone && npm run verify:no-vscode-in-standalone && npm run build:standalone && npm run test:standalone"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^25.0.3",
    "@types/vscode": "^1.80.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.3.8",
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "semantic-release": "^25.0.2",
    "typescript": "^5.3.0",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@slack/web-api": "^7.13.0",
    "@toon-format/toon": "^2.1.0",
    "nano-spawn": "^2.0.0",
    "smol-toml": "^1.6.0"
  }
}
